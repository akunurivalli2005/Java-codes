// Month-1_CoreJava/Day13_Variables/VariablesExample.java
// Day 13: Variables in Java
// Topic: Understanding different types of variables

public class VariablesExample {

    // Instance variable (non-static, belongs to object)
    int instanceVar = 50;  // Can be accessed using object

    // Static variable (class variable, shared by all objects)
    static int staticVar = 100;  // Can be accessed using class name

    public static void main(String[] args) {

        // Local variable (declared inside method)
        int localVar = 20;  // Accessible only inside main method
        System.out.println("Local Variable: " + localVar);
        // Prints the value of localVar

        // Accessing static variable
        System.out.println("Static Variable: " + VariablesExample.staticVar);
        // Static variables are accessed using class name

        // Creating object to access instance variable
        VariablesExample obj = new VariablesExample();  // Creates object 'obj'
        System.out.println("Instance Variable: " + obj.instanceVar);
        // Prints the value of instanceVar using object
    }
}
output:
Local Variable: 20
Static Variable: 100
Instance Variable: 50

